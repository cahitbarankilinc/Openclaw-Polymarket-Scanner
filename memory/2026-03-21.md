# 2026-03-21

- User provided a strong practical constraint for Etsy automation: API/draft uploads may technically work but can perform poorly for traffic/sales compared with manual human-like listing flows.
- Research direction changed accordingly: prioritize tools for human-like Etsy screen/browser automation beyond Playwright, with macOS teaching/testing and Windows VPS execution as the target workflow.
- New hourly cron created: `etsy-humanlike-bot-research` (id: `5a93b083-93f8-41ed-9738-fa10cefc1288`).
- Additional durable Etsy automation architecture note from the 04:04 research cycle: for mac-teach -> Windows-VPS-run workflows, the strongest current last-mile candidate class is Windows-native attended RPA (especially Power Automate Desktop or UiPath), optionally augmented by Stagehand/Skyvern for adaptive browser reading and AutoHotkey/SikuliX for dialog-level recovery. Durable implication: prefer a Windows desktop/RPA-centered publish runner over a pure browser-agent stack for Etsy listing work.

- Durable Etsy human-like automation preference reinforced by the 05:04 research cycle: for mac-teach -> Windows-VPS-run workflows, the current best POC order is Power Automate Desktop first, UiPath second, with Stagehand/Skyvern as helper browser intelligence rather than the main runner; keep visual tools like SikuliX/AutoHotkey in the recovery layer, not the core orchestration layer.
- Durable Etsy human-like automation note from the 06:04 research cycle: Microsoft Learn now gives explicit evidence that Power Automate Desktop supports virtual desktop/RDP automation plus screenshot, debug pause, and safe-stop workflows. This strengthens the project’s current POC order: PAD first on Windows VPS, UiPath second, with AutoHotkey reserved for native dialog/focus recovery rather than as the main runner.

- Durable Etsy human-like automation preference updated by the 07:04 research cycle: keep Power Automate Desktop as the first Windows-VPS attended POC runner because Microsoft docs now give the clearest recorder/debug/safe-stop/virtual-desktop evidence; treat UiPath as the escalation path when Etsy UI fragility or selectorless vision-based control makes PAD + AutoHotkey insufficient.
- Durable Etsy human-like automation note from the 08:03 research cycle: Microsoft’s own PAD troubleshooting docs explicitly say browser-opened Save/Open/Upload dialogs cannot be automated via PAD web recorder/web automation actions and require desktop/UI automation workarounds. Project implication: keep native upload/focus recovery as a first-class Windows layer (PAD + AutoHotkey first), and treat browser-only helpers like Stagehand as auxiliary rather than the main Etsy publish runner.

- Durable Etsy automation architecture preference reinforced by the 09:02 research cycle: use a recorder-first Power Automate Desktop POC on Windows VPS, then harden brittle steps manually; escalate to UiPath specifically when duplicate form fields, selector fragility, or attended screenshot/retry ergonomics justify the added complexity.

- Durable Etsy human-like automation preference updated by the 10:03 research cycle: keep PAD as the recorder-first Windows-VPS runner, but treat UiPath as the explicit escalation path when duplicate-label form sections, anchor-heavy targeting, or structured retry/handoff needs make PAD + AutoHotkey maintenance too brittle.
