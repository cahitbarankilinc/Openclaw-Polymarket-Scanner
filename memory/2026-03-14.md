# 2026-03-14

- Polymarket weather scanner: integrated open-position loss handling into wallet win/bucket stats. Only open positions with `percentPnl` between `-101` and `-95` inclusive count, and they are treated as synthetic losses bucketed by `avgPrice`.
- Found major UX/data consistency issues during rollout: seed-only placeholder rows were surfacing in UI as if analyzed, causing false `Open loss: 0` readings.
- Fixed scanner result selection so seed-only rows are hidden from latest dashboard results, and latest analyzed rows take precedence over older historical qualified rows for the same wallet.
- Discovered root cause for user-facing stale `Open loss: 0` after backend was correct: dashboard frontend assets were being served from cache. Added no-cache headers for static files and versioned `app.js` include to bust stale frontend caches.
- Reworked scanner architecture so listed wallets are actually analyzed, instead of only a per-category top subset. Removed the earlier seed/stage placeholder-first dashboard behavior.
- Performance improvement: wallet evaluation now fetches `total_markets_traded`, `user_activity`, `closed_positions_all`, and `open_positions` in parallel per wallet, then computes one combined result.
- User preference/constraint surfaced strongly: do not stop at partial fixes; verify end-to-end (DB -> API -> UI) with concrete wallet spot checks before claiming success.
